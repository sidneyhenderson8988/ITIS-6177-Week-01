// var http = require('http');

// http.createServer(function (req, res) {
//   res.writeHead(200, {'Content-Type': 'text/plain'});
//   res.end('Hello World!');
// }).listen(3000);

// NOTE: I wasn't sure if the professor wanted the "Hello World" example to be a server,
// so I included that portion above. Below is the console.log() the professor
// asked for in the video that explains Exercise 1.

console.log("Hello World!")